{
  "name": "kailian",
  "private": true,
  "version": "0.1.0",
  "description": "开练 — UniApp + Node.js 健身训练记录",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup": "npm install && npm run db:setup -w api",
    "dev": "concurrently -n api,uni -c cyan,magenta \"npm run dev -w api\" \"npm run dev:h5 -w uni\"",
    "dev:api": "npm run dev -w api",
    "dev:h5": "npm run dev:h5 -w uni",
    "dev:mp-weixin": "npm run dev:mp-weixin -w uni",
    "build": "npm run build:h5 -w uni && npm run build -w api",
    "db:setup": "npm run db:setup -w api",
    "db:seed": "npm run db:seed -w api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
